# Núcleo
discord.py>=2.7.1
aiohttp>=3.13
python-dotenv>=1.0

# Voz — recepción de audio de Discord + descifrado E2E (DAVE)
discord-ext-voice-recv>=0.5.2a0
davey>=0.1

# STT — transcripción con Groq Whisper API (sustituye a Vosk + faster-whisper)
groq>=0.4

# TTS — síntesis de voz local con Piper
piper-tts>=1.4
numpy>=1.26

# audioop se eliminó de la stdlib en Python 3.13; este backport lo restituye.
# Inofensivo en versiones anteriores.
audioop-lts>=0.2; python_version >= "3.13"
